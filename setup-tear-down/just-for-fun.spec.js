it('just for fun', () => {
  const gab = 'A genius of typing scratch and programming'
  expect(gab).toBe('A genius of typing scratch and programming')
})

it('just for fun', () => {
  const Dan = 'A genius of typing scratch and programming'
  expect(Dan).toBe('A genius of typing scratch and programming')
})

it('just for fun', () => {
  const dominica = 'A genius of cute'
  expect(dominica).toBe('A genius of cute')
})

it('just for fun', () => {
  const cristophor = 'A genius of typing scratch and programming'
  expect(cristophor).toBe('A genius of typing scratch and programming')
})

it('just for fun', () => {
  const momi = 'A genius of cooking'
  expect(momi).toBe('A genius of cooking')
})